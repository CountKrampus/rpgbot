Yes. I traced the current search.py on your GitHub. The function currently responsible for Progress: is get_search_progress(). It is currently scraping the entire <body> and taking the first number/number pair it finds, which is exactly why it can display something nonsensical like 561/15.
Replace this entire function
Find:
def get_search_progress(driver):

    try:

        text = driver.find_element(
            By.TAG_NAME,
            "body"
        ).text

        matches = re.findall(
            r"\b(\d+)\s*/\s*(\d+)\b",
            text
        )

        if matches:

            current, maximum = matches[0]

            return (
                int(current),
                int(maximum)
            )

    except Exception:

        pass

    return (
        None,
        None
    )
Replace it with this
def get_search_progress(driver):
    """
    Read the CURRENT MAP's real search progress directly
    from Eclipse RPG.

    The map page contains:

        <td class="tnav map-stat">
            <div id="times-searched">
                <div id="times-searched-number">651</div>/2,500
            </div>
        </td>

    Returns:

        (651, 2500)

    instead of searching the entire page for the first
    number/number combination.
    """

    try:

        # ----------------------------------------------------
        # Current searches completed.
        # ----------------------------------------------------

        current_element = driver.find_element(
            By.ID,
            "times-searched-number"
        )

        current_text = (
            current_element.text
            .strip()
            .replace(",", "")
        )

        if not current_text:
            return (
                None,
                None
            )

        current = int(
            current_text
        )

        # ----------------------------------------------------
        # Find the parent #times-searched element.
        #
        # The maximum is text outside the child div:
        #
        #     <div id="times-searched-number">651</div>/2,500
        # ----------------------------------------------------

        progress_element = driver.find_element(
            By.ID,
            "times-searched"
        )

        progress_text = (
            progress_element.text
            .strip()
        )

        # Example:
        #
        #     651/2,500
        #
        match = re.search(
            r"/\s*([\d,]+)",
            progress_text
        )

        if not match:

            return (
                current,
                None
            )

        maximum = int(
            match.group(1).replace(
                ",",
                ""
            )
        )

        return (
            current,
            maximum
        )

    except (
        NoSuchElementException,
        ValueError,
        WebDriverException,
    ):

        return (
            None,
            None
        )
After this change
With the HTML you gave me:
HTML
<div id="times-searched-number">651</div>/2,500
your bot will display:
Progress: 651/2500
  Search 180/300
instead of trying to guess which x/y on the page represents map progress.
And there's an important distinction in your current run_searches():
current, maximum = get_search_progress(driver)
is only used for the displayed Progress: line. The separate:
search_number
controls your automation run's Search 180/300 counter.
So this replacement fixes the display without accidentally changing how many searches your bot performs.
One thing I'd change next: now that we're reading the real 2,500 map limit, we can make run_searches() automatically stop when Eclipse reports 2,500/2,500, rather than relying entirely on the manually supplied searches value. That would make map completion actually work the way you intended.


Yes. I checked the current search.py on GitHub, and we can do this without changing your capture system.
Your current search.py already records every encountered Pokémon in _encountered_pokemon["by_name"].
The cleanest approach is to have handle_search_encounter() record the Pokémon as caught only when capture_encounter() returns True. That means we don't have to guess based on capture statistics.
1. Replace get_encountered_pokemon_stats()
Find the existing:
def get_encountered_pokemon_stats():
and replace the entire function with:
def get_encountered_pokemon_stats():
    """
    Return a copy of the current session's encountered-Pokemon
    statistics.

    Includes:

        total
        by_name
        rare
        caught
        caught_total

    Example:

        {
            "total": 47,
            "by_name": {
                "Shellder": 12,
                "Slowpoke": 8,
            },
            "rare": [
                {
                    "name": "Shiny Goldeen",
                    "level": 22,
                },
            ],
            "caught": {
                "Shellder": 10,
                "Slowpoke": 7,
            },
            "caught_total": 17,
        }
    """

    return {
        "total": _encountered_pokemon["total"],

        "by_name": dict(
            _encountered_pokemon["by_name"]
        ),

        "rare": list(
            _encountered_pokemon["rare"]
        ),

        "caught": dict(
            _encountered_pokemon.get(
                "caught",
                {}
            )
        ),

        "caught_total": sum(
            _encountered_pokemon.get(
                "caught",
                {}
            ).values()
        ),
    }
2. Replace reset_encountered_pokemon_stats()
Find:
def reset_encountered_pokemon_stats():
Replace the entire function with:
def reset_encountered_pokemon_stats():

    _encountered_pokemon["total"] = 0

    _encountered_pokemon["by_name"] = {}

    _encountered_pokemon["rare"] = []

    _encountered_pokemon["caught"] = {}
This doesn't require you to modify the _encountered_pokemon dictionary at the top because we're using .get()/creating "caught" dynamically.
3. Replace handle_search_encounter()
This is the important one.
Find:
def handle_search_encounter(driver):
and replace the entire function with:
def handle_search_encounter(driver):

    encountered = get_encounter_pokemon(
        driver
    )

    if encountered:

        rarity_marker = (
            " ★ RARE"
            if is_rare_pokemon(
                encountered["name"]
            )
            else ""
        )

        print(
            f"  Wild {encountered['name']} "
            f"Lv. {encountered['level']}"
            f"{rarity_marker}"
        )

        _record_encounter(
            encountered
        )

    if not click_encounter_fight(
        driver
    ):

        print(
            "✗ Could not start encounter."
        )

        return False

    result = capture_encounter(
        driver
    )

    if result:

        print(
            "✓ Encounter capture completed."
        )

        # ----------------------------------------------------
        # Record the actual Pokémon as caught.
        #
        # capture_encounter() only returns True after the
        # Pokémon has actually been captured successfully.
        # ----------------------------------------------------

        if encountered:

            caught = _encountered_pokemon.setdefault(
                "caught",
                {}
            )

            pokemon_name = encountered[
                "name"
            ]

            caught[pokemon_name] = (
                caught.get(
                    pokemon_name,
                    0
                ) + 1
            )

        return True

    print(
        "✗ Encounter capture failed."
    )

    return False
What this gives you
Now if your 300-search session encounters:
Gyarados
Gyarados
Pidgey
Shiny Gyarados
Gyarados
Pidgey
and catches:
Gyarados
Gyarados
Shiny Gyarados
Pidgey
the statistics will contain:
caught:
    Gyarados       2
    Shiny Gyarados 1
    Pidgey         1
So we can make the end-of-session screen show:
============================================================
POKEMON CAUGHT THIS SESSION
============================================================

Gyarados                 x2
Shiny Gyarados            x1
Pidgey                     x1

------------------------------------------------------------
Total Pokémon caught: 4
============================================================
And because we're using the actual encounter name, variants stay separate:
Gyarados
Shiny Gyarados
Crystal Gyarados
Golden Gyarados
rather than all being lumped together as Gyarados.
One more change is needed
Those three replacements collect the data, but your current search_complete_menu() doesn't display it yet. The current search session records its search totals separately through _record_search_session().
So the next function to replace is search_complete_menu() itself. That's where I'd put the nice summary:
SEARCHES COMPLETE

Map: Pokemon Mansion
Searches this session: 300

POKEMON CAUGHT
------------------------------------------------------------
Gyarados                  x2
Shiny Gyarados             x1
Pidgey                     x1
------------------------------------------------------------
Total caught:              4
Total encounters:          7
Capture rate:           57.1%
That part can be added without touching the working capture code.
